I'll help you find a solution for the failing job. Let me start by getting the logs for that job.Now let me look at the test file to understand what's failing:## Solution for Failing Job 69568992500

The test failures are caused by **missing `turn_index.json` files** in temporary test directories. The root issue is that several tests are attempting to read `turn_index.json` immediately after calling `extractStoryData()`, but the extraction is failing silently or the file is not being created.

### Primary Issues

1. **Lines 338, 367, 399, 512, 658**: `ENOENT: no such file or directory, open '/tmp/test-xxx/turn_index.json'`
   - Tests read `turn_index.json` after extraction but the file doesn't exist
   - This suggests `extractStoryData()` is not creating the expected output files

2. **Lines 59, 286, 315, etc.**: File operations depend on `turn_index.json` existing
   - After extraction completes, subsequent file reads assume the JSON files are present

### Root Cause

The `extractStoryData()` function is likely failing to create the required JSON files in the output directory. Check the extraction handler for:
- Missing directory creation logic
- File write errors being swallowed
- Improper temp directory cleanup between tests

### Recommended Fix

Modify `test/handlers/query.test.js` to validate extraction success before proceeding:

```javascript
test('queryStoryData - queries manifest category', async (t) => {
  const tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), 'test-'));
  const inputFile = path.join(testFilesDir, 'TheWorldsAStageTurn4.txt');

  // Extract first and validate
  const extractResult = await extractStoryData([inputFile], tmpDir);

  // ADD THIS VALIDATION
  if (!extractResult.success) {
    fs.rmSync(tmpDir, { recursive: true });
    throw new Error(`Extraction failed: ${extractResult.error}`);
  }

  // Verify required files exist
  const turnIndexPath = path.join(tmpDir, 'turn_index.json');
  if (!fs.existsSync(turnIndexPath)) {
    fs.rmSync(tmpDir, { recursive: true });
    throw new Error(`turn_index.json not created at ${turnIndexPath}`);
  }

  // Query manifest
  const result = await queryStoryData(tmpDir, 'manifest', []);
  // ... rest of test
});
```

### Additional Check

Verify that `lib/handlers/extraction.js` is:
1. Creating the output directory if it doesn't exist
2. Writing all required JSON files (manifest.json, turn_index.json, metadata.json)
3. Returning a success status that the tests can validate

The test file path hardcoded at line 13 (`/home/moose/personalProjects/...`) also needs to be updated to a relative or environment-based path for CI/CD environments.
